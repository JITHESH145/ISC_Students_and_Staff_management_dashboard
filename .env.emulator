# Loaded by `npm run dev:emulator` (vite --mode emulator).
# Points the app at the local Firebase Emulator Suite instead of the live
# project, so all reads/writes stay on your machine. Safe to commit — no secrets.
VITE_USE_EMULATOR=true
